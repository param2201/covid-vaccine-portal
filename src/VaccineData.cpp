#include "VaccineData.h"
#include<bits/stdc++.h>
using namespace std;

VaccineData :: VaccineData(string district, string center, int pin, int slot){
        this -> district = district;
        centerName = center;
        pincode = pin;
        this -> slot = slot;
}
